<UserControl x:Class="ImageViewer.Views.Filter.EnumFilterParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageViewer.Views.Filter"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <!--<UserControl.Resources>
        <DataTemplate x:Key="EnumListItemView">
            <TextBlock Text="{Binding }"/>
        </DataTemplate>
        ItemTemplate="{StaticResource EnumListItemView}"
    </UserControl.Resources>-->
    <ComboBox
        Margin="{StaticResource SmallBorderBottom}"
        SelectedIndex="{Binding Value}"
        ItemsSource="{Binding EnumValues}"
        />
</UserControl>
